# Backend API base URL (must end with /api/)
NEXT_PUBLIC_API_URL=http://localhost:5001/api/

# Socket.IO URL (typically the same host as the API, without /api/)
NEXT_PUBLIC_SOCKET_URL=http://localhost:5001

# Optional hostname where the legal footer should be hidden.
NEXT_PUBLIC_HIDE_LEGAL_FOOTER_HOSTNAME=
